// Code generated by gowasm. DO NOT EDIT.
//
// tsc copies only .ts output, but the package also ships the compiled module
// and the Go runtime bridge. Both are resolved relative to the *compiled* file
// via new URL(..., import.meta.url), so they must land beside it in dist/.
import { cp, mkdir } from "node:fs/promises";

const assets = [
  ["src/main.wasm", "dist/main.wasm"],
  ["src/vendor/wasm_exec.js", "dist/vendor/wasm_exec.js"],
];

await mkdir("dist/vendor", { recursive: true });
for (const [from, to] of assets) {
  await cp(from, to);
}
