# This whole directory is generated by `gowasm build`.
node_modules/
dist/

# Whichever package manager built it left one of these. They describe a
# generated directory, so there is nothing for them to pin.
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lockb
bun.lock
