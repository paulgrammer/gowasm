// Code generated by gowasm. DO NOT EDIT.
//
// The `{{.NS}}` namespace: everything the Go package `{{.PkgName}}` exports.
// One module serves both import forms, so there is nothing to keep in sync:
//
//   import { {{.NS}} } from "{{.Package}}";
//   import { {{.First}} } from "{{.Package}}/{{.NS}}";
import { bind, type {{.APIName}} } from "./generated/{{.NS}}/client.js";
import { sharedRuntime } from "./runtime/instance.{{.Target}}.js";

export * from "./generated/{{.NS}}/types.js";
export type { {{.APIName}} };

// A function whose JavaScript name is a reserved word has no named export here,
// because `export const new` is a syntax error. It is still on the client
// object returned by createClient().
{{range .Funcs}}{{if not .Reserved}}
export const {{.JSName}}: {{$.APIName}}["{{.JSName}}"] = (...args) =>
  sharedRuntime().then((rt) => bind(rt).{{.JSName}}(...args));
{{- end}}{{end}}
