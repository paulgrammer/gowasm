// Code generated by gowasm. DO NOT EDIT.
//
// Ambient declarations for the Go runtime bridge vendored from
// $(go env GOROOT)/lib/wasm/wasm_exec.js, which defines exactly one global.
export {};

declare global {
  class Go {
    argv: string[];
    env: Record<string, string>;
    /** Invoked BY the Go runtime on exit. Not a method you call to stop it. */
    exit: (code: number) => void;
    readonly importObject: WebAssembly.Imports;
    exited: boolean;
    /** Settles when the Go program's main() returns. */
    run(instance: WebAssembly.Instance): Promise<void>;
  }

  // wasm_exec.js polyfills these when absent; declared so the Node loader can
  // install the real modules first.
  // eslint-disable-next-line no-var
  var fs: unknown;
  // eslint-disable-next-line no-var
  var path: unknown;
}
